export declare const WEB_STORAGE_DEFAULT_CONFIG: WebStorageConfig;
export interface WebStorageConfig {
    prefix: string;
}
export declare class ConfigManager {
    private static _config;
    static readonly prefix: string;
    static set(value?: WebStorageConfig): void;
}
export declare class KeyUtility {
    static compose(key: string): string;
    static isComposite(key: string): boolean;
}
