import { Observable } from 'rxjs';
import { WebStorageItemAction } from './web-storage.service';
import { WebStorage } from './web-storage';
export declare class WebStorageController extends WebStorage {
    private _actions;
    private _errors;
    readonly actions: Observable<WebStorageItemAction>;
    readonly errors: Observable<any>;
    get(key: string): any;
    set(key: string, value: any): boolean;
    remove(key: string): boolean;
    clear(): void;
    private _notify;
}
